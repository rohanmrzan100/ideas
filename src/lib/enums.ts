export enum PLAN_TYPE {
  FREE = 'free',
  PRO = 'pro',
  ENTERPRISE = 'enterprise',
}

export enum SUBSCRIPTION_STATUS {
  TRIALING = 'trialing',
  ACTIVE = 'active',
  PAST_DUE = 'past_due',
  CANCELED = 'canceled',
  EXPIRED = 'expired',
}
export enum ROLES {
  SELLER = 'seller',
  ADMIN = 'admin',
}
